import { createTheme } from '@mui/material';

export const theme = createTheme({
    palette: {
        primary: { main: '#6c6c6c' },
        secondary: { main: '#a1ff04' },
    },
    typography: {
        fontFamily: 'Roboto, Arial, sans-serif',
    },
});